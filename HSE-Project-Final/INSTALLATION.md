# دليل التثبيت والتشغيل

## 📦 التثبيت السريع

### الخطوة 1: فتح المشروع
```bash
# افتح المجلد HSE-Project-Final في محرر الكود
cd HSE-Project-Final
```

### الخطوة 2: تشغيل خادم محلي
يجب استخدام خادم HTTP محلي لأن ES6 Modules لا تعمل مع `file://` protocol.

#### الطريقة 1: Python (موصى به)
```bash
# Python 3
python -m http.server 8000

# أو Python 2
python -m SimpleHTTPServer 8000
```

#### الطريقة 2: Node.js
```bash
# تثبيت http-server
npm install -g http-server

# تشغيل الخادم
http-server -p 8000
```

#### الطريقة 3: VS Code Live Server
1. تثبيت إضافة "Live Server" في VS Code
2. انقر بزر الماوس الأيمن على `index.html`
3. اختر "Open with Live Server"

### الخطوة 3: فتح المتصفح
افتح المتصفح واذهب إلى:
```
http://localhost:8000
```

## ☁️ النشر على Netlify

### الخطوة 1: إعداد الملفات
تأكد من وجود الملفات التالية في المجلد:
- `index.html`
- `app.js`
- `styles.css`
- `manifest.json`
- `_redirects`
- `netlify.toml`

### الخطوة 2: رفع الملفات
1. سجل الدخول إلى [Netlify](https://www.netlify.com)
2. اذهب إلى "Sites"
3. اسحب المجلد `HSE-Project-Final` إلى Netlify
4. أو استخدم Git:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin YOUR_REPO_URL
   git push -u origin main
   ```

### الخطوة 3: إعدادات Netlify
- **Build command:** (اتركه فارغاً)
- **Publish directory:** `HSE-Project-Final`

## 📊 إعداد Google Sheets

### الخطوة 1: إنشاء Google Spreadsheet
1. اذهب إلى [Google Sheets](https://sheets.google.com)
2. أنشئ Spreadsheet جديد
3. سجل ID الخاص به من الرابط

### الخطوة 2: إعداد Google Apps Script
1. افتح Spreadsheet
2. اذهب إلى `Extensions` > `Apps Script`
3. احذف الكود الافتراضي
4. انسخ محتوى `google-apps-script.js` أو الملفات من `google-apps-script-modules`
5. احفظ المشروع

### الخطوة 3: نشر Web App
1. انقر على `Deploy` > `New deployment`
2. اختر `Web app`
3. حدد الإعدادات:
   - **Execute as:** Me
   - **Who has access:** Anyone
4. انقر `Deploy`
5. انسخ رابط الويب (Web App URL)

### الخطوة 4: إعداد النظام
1. سجل الدخول إلى النظام
2. اذهب إلى `الإعدادات`
3. أدخل:
   - **Google Spreadsheet ID**
   - **Google Apps Script URL**
4. فعّل `Google Sheets Integration`
5. احفظ الإعدادات

## 🔐 تغيير بيانات الدخول الافتراضية

### الطريقة 1: من خلال النظام
1. سجل الدخول كمدير
2. اذهب إلى `المستخدمين`
3. عدّل بيانات المستخدم
4. غيّر كلمة المرور

### الطريقة 2: من خلال Google Sheets
1. افتح Google Spreadsheet
2. اذهب إلى ورقة `Users`
3. غيّر كلمة المرور (سيتم تشفيرها تلقائياً)

## ⚙️ إعدادات إضافية

### إضافة شعار الشركة
1. سجل الدخول كمدير
2. اذهب إلى `الإعدادات`
3. ارفع شعار الشركة
4. احفظ الإعدادات

### تخصيص الألوان
عدّل ملف `styles.css`:
```css
:root {
    --primary-color: #your-color;
    --secondary-color: #your-color;
}
```

## 🔍 استكشاف الأخطاء

### المشكلة: الصفحة لا تعمل
**الحل:** تأكد من استخدام خادم HTTP وليس `file://`

### المشكلة: Google Sheets لا يعمل
**الحل:**
1. تحقق من صحة Spreadsheet ID
2. تحقق من صحة Web App URL
3. تأكد من نشر Web App بشكل صحيح
4. تحقق من الصلاحيات

### المشكلة: تسجيل الدخول لا يعمل
**الحل:**
1. تحقق من وجود المستخدمين في قاعدة البيانات
2. تحقق من Google Sheets Integration
3. امسح localStorage و IndexedDB وأعد المحاولة

## 📝 ملاحظات

- تأكد من نسخ جميع الملفات بشكل صحيح
- تأكد من وجود جميع الملفات المطلوبة
- في حالة المشاكل، راجع ملف `README.md`

---

**تم التحديث:** $(Get-Date -Format "yyyy-MM-dd")

